# Это аргументы командной строки для запуска виртуальной машины Java (JVM) с использованием опций для установки размеров памяти, резервирования кодового кеша, настройки компилятора и выбора алгоритма сборки мусора. Конкретно, это:

# - `-Dfile.encoding=UTF8`: устанавливает кодировку файла по умолчанию как UTF-8.
# - `-Xms1G`: устанавливает начальный размер памяти для Java Heap на 1 гигабайт.
# - `-Xmx5G`: устанавливает максимальный размер памяти для Java Heap на 5 гигабайт.
# - `-XX:ReservedCodeCacheSize=500M`: резервирует 500 мегабайт для кодового кеша, который используется для хранения скомпилированных классов.
# - `-XX:+TieredCompilation`: включает поэтапную компиляцию кода с использованием различных уровней оптимизации для ускорения работы программы.
# - `-XX:+UseParallelGC`: устанавливает алгоритм сборки мусора для управления памятью с использованием параллельных потоков.

-Dfile.encoding=UTF8
-Xms1G
-Xmx5G
-XX:ReservedCodeCacheSize=500M
-XX:+TieredCompilation
-XX:+UseParallelGC